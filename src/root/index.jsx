import React from "react";
import { Router } from "react-router-dom";
import Navbar from "../components/Navbar";

export const Root = () => {
  return (
    <div>
      <Navbar />
    </div>
  );
};
export default Root;
